syntax = "proto3";

package metric;

option go_package = "keti/ai-storage-metric-collector/pkg/proto";

service MetricCollector {
  rpc GetMultiMetric(Request) returns (MultiMetric); 
}

message Request {}

message MultiMetric {
  string node_name = 1;
  int32 metric = 2;
}